# PI Agent · 长期自主目标（2026-08-18 起）

> 模式：小羽自主推进，自动化验证通过后再进下一步；无法自动验证的（视觉/手感）集中交东哥验收。
> 优先级来自 202608181539 调研文档。

## M4 · 上下文管理（P1，同类标配）✅ 2026-08-18
- [x] 状态栏：token / cost / context 占用百分比（真实 PI 数据）✅
- [x] 手动 compact 按钮 ✅
- [x] 思考级别（thinking level）下拉调节 ✅
- [x] 自动测试：get_session_stats 真实数据验证（522 消息/255 工具调用/token 3557万/$0.43/context 25.8%）✅

## M5 · Composer 增强（P2）✅ 2026-08-18（斜杠补全部分）
- [x] 斜杠命令补全提示：输入 / 弹出列表（42 技能 + 14 内建命令），↑↓ 选择、Tab/Enter 确认、Esc 关闭 ✅
- [x] `/skill:name` 技能即选即发 ✅
- [ ] `@` 文件引用（模糊搜索项目文件）
- [ ] `!cmd` / `!!cmd` shell 命令前缀支持

## M6 · 会话管理增强（P2）✅ 2026-08-18（重命名/删除部分）
- [x] 会话重命名（切目标 → setName）✅
- [x] 会话删除（confirm 二次确认 + sidecar 删除文件）✅
- [x] 修复：模型下拉重复 key（同 id 去重 + provider:id key）✅（错误转发抓到）
- [ ] fork / clone（任意历史消息分支）
- [ ] 会话树（/tree）跳转视图

## M7 · 三栏布局 + 工具时间线（成熟范式）✅ 2026-08-18
- [x] 右信息面板：工具调用时间线（名称/状态/参数/结果，可折叠）✅
- [x] 主区对话流去噪（工具详情收敛到右栏）✅
- [x] 事件模型测试：工具时间线构建/错误标记/消息流折叠 3/3 ✅

## M8 · 差异化（护城河）✅ 2026-08-18（皮肤/技能参数化/宠物占位）
- [x] 皮肤系统深化：四套主题 token（浅色/深色/暖阳/薄荷）+ 注册表化 ✅
- [x] 技能参数化（轻量版）：点击技能弹窗带描述 + 可选附加指令 → `/skill:name 附加` ✅
- [x] 桌面宠物占位：独立透明置顶窗口 + CSS 动画小幽灵（pet.html + pet.rs 接口）✅
- [ ] 中文母语化体验打磨（跟随各功能进行）

## M9 · 工程化收尾
- [x] Windows 安装包构建 ✅ 2026-08-18：`PI Agent_0.1.0_x64-setup.exe`（NSIS 2.0MB）+ `.msi`（3.1MB）+ release exe（9.1MB）
- [x] 测试套件整理 ✅：`npm test`（vitest 12/12：核心链路4 + 事件模型3 + sidecar 协议5）、`test:sidecar`/`test:full`/`test:all`
- [x] 网页版部署验证 ✅：`vite preview` 200 可访问；非 Tauri 环境优雅降级（网页预览徽标 + piSend 拒绝）
- [x] 正式版重新打包（含三栏/宠物/主题/网页防护）— 进行中

## 自主工作纪律
1. 每一步：改代码 → 自动验证（sidecar 脚本 / tsc / vitest）→ 通过才进下一步
2. 无法自动验证的（窗口视觉、交互手感、真实模型体验）→ 汇总清单，最后交东哥测试
3. 重大方向调整 → 先汇报东哥

## M10 · 设置面板 ✅ 2026-08-18
- [x] 侧栏底部「⚙ 设置」按钮 → 模态设置面板（3 tab）
- [x] 模型提供商管理：40 个内置 provider 列表（搜索）+ 认证状态徽标 + API Key 持久化（写 ~/.pi/agent/auth.json，终端共享，原子写入）
- [x] 通用设置（写真实 settings.json，终端共享）：默认模型 / 默认思考级别 / 自动压缩 / 自动重试 / 隐藏思考块
- [x] 关于：模型数 / 提供商数 / 已认证数
- [x] 自动测试：get_providers / get_settings / set_setting 协议回归 ✅

## M10.5 · 侧栏重构 + 设置面板左右布局 ✅ 2026-08-18
- [x] 侧栏顺序：项目 → 会话 → 技能库（技能库不再显示插件信息）
- [x] 项目区「＋ 添加项目」：输入路径 → switch_project 并刷新（自动发现会话）
- [x] 会话区「＋ 新建会话」快捷按钮（保留顶部主按钮）
- [x] 设置面板改左右布局：左侧垂直标签页（模型提供商/通用设置/插件/关于）+ 右侧内容
- [x] 设置新增「插件」tab：列出真实 PI 扩展 + 「⟳ 重载资源」（等价终端 /reload）
- [x] sidecar `reload_resources` 指令（resourceLoader.reload()）验证通过

## M10.6 · 添加项目对话框（UI 风格 + 目录浏览）✅ 2026-08-18
- [x] 添加项目改为 UI 风格模态对话框（不再是系统 prompt）
- [x] 「📁 浏览…」按钮：Tauri dialog 插件原生文件夹选择器（tauri-plugin-dialog，capability 已配）
- [x] 支持手动输入路径 + Enter 确认；错误提示（路径不可用）UI 内显示
- [x] 添加后：switch_project → 刷新项目列表/会话列表/消息（新项目独立管理其会话）
- [x] 修复：cargo 编译失败（dev 窗口占用 exe）排查——先杀窗口再编译

## M10.7 · 项目-会话隔离修复 ✅ 2026-08-18（重要 bug）
- [x] **根因**：switch_project 后 list_sessions 用 agent.state.cwd（不存在）→ fallback 进程目录 → 永远列出 PI-GUI 会话，无法分别管理各项目对话
- [x] **修复**：sidecar 维护 currentCwd（handleInit 设置），list_sessions 优先用它
- [x] 验证：PI-GUI 1 会话 → 切 dongdong 14 会话（正确切换）→ 新建会话落 dongdong 目录
- [x] 新增自动化回归测试：项目隔离（switch 后 list_sessions 不串项目）

## M11 · 视图缩放 + 项目树状会话 ✅ 2026-08-18
- [x] Ctrl/Cmd + +/-/0 缩放 UI（CSS zoom 0.7~1.6，localStorage 持久化）
- [x] sidecar `list_all_sessions`：按项目分组列出全部会话 + 未分类（无 cwd 归属）
- [x] 侧栏树状重构：项目节点可折叠展开（▾/▸）其会话；当前项目默认展开
- [x] 顶部「未分类会话」区：只显示不归属任何项目的会话（当前 0 个，PI 会话天然按项目存储）
- [x] 项目展开后「＋ 在此项目新建会话」（自动先切项目再新建）

## M12 · 模型 provider 区分 + API Key 核对 ✅ 2026-08-18
- [x] 核对：opencode-go 的 API Key 已是东哥给的（67 字符一致），无需切换
- [x] 根因：deepseek 官方与 opencode-go 各有同名 deepseek-v4-flash/pro，前端按 id 去重导致只显示 1 个且无法分辨 provider
- [x] 修复：去重键改 provider:id；模型下拉标注提供商（name（provider））；选择用 provider/id 精确定位
- [x] sidecar set_model 支持 provider+modelId 精确匹配
- [x] 验证：opencode-go/deepseek-v4-flash 与 deepseek/deepseek-v4-flash 可分别切换；两家模型齐全（deepseek 2 + opencode-go 19）
- [x] 设置面板默认模型下拉同样标注 provider

## M13 · 侧栏布局修复 + 项目/会话交互增强 ✅ 2026-08-18
- [x] 侧栏：内容包入可滚动容器，footer（设置/主题）flex-shrink:0 固定底部，不再跑到上面
- [x] 输入框修复：chat__inputbar / ToolBar flex-shrink:0，防被压缩隐藏
- [x] 项目节点 hover 显示「＋」：点击即在该项目新建会话（自动先切项目）
- [x] 会话右键菜单：打开/置顶/重命名/复制路径/删除（置顶 localStorage 持久化，置顶会话排前）

## M13.5 · 输入框「看不见」诊断 ✅ 2026-08-18
- [x] 运行时布局诊断（getBoundingClientRect 输出到日志）：inputbar 实际存在且尺寸正常（1221×90，位于 scroll 正下方）——布局本身无 bug
- [x] 结论：用户环境（窗口矮 / zoom 缩放 / DPI）导致底部被 body overflow:hidden 裁剪的可能性最大
- [x] 加固：.chat / .chat__main-left 加 overflow:hidden 防内部溢出；inputbar / ToolBar flex-shrink:0
- [x] 建议用户：若仍不可见 → Ctrl+0 重置缩放 + 拉高窗口

## M13.6 · 缩放重构 + 侧栏拖拽 + 无响应诊断 ✅ 2026-08-18
- [x] 缩放逻辑重构：弃用 CSS zoom（会把布局裁出视口），改为 html 基准字号 + rem（94 处 font-size 批量转 rem）——只缩放文本，布局自适应
- [x] 侧栏拖拽改宽：右缘 6px 手柄，180~420px，localStorage 持久化
- [x] 无响应诊断：连接自检实测 models=369、state 正常（deepseek-v4-flash/deepseek 无错误）——当前代码窗口健康
- [x] 结论：用户截图窗口为旧实例（提示「Ctrl+滚轮 缩放」非本代码），发送无响应与模型空均源于旧实例

## M14 · 模型列表空/下拉不显示 修复 ✅ 2026-08-18
- [x] 根因：窗口启动时 loadAll 在 sidecar init 完成前执行 → get_available_models/get_state 返回「尚未 init」→ 模型列表静默为空 → 下拉空白
- [x] 修复：loadAll 自动重试（模型空或状态失败时每 2s 重试，最多 8 次；切项目时重置计数）
- [x] 验证：启动时序 [fail 尚未init → fail → ok]，第三次加载 369 模型 + 4 项目全部成功

## M14.5 · 模型下拉体验完善 + 事件流确认 ✅ 2026-08-18
- [x] 确认事件流完整（agent_start/turn/message/tool 全类型，busy 状态正常）——解包修复后发送/回复/工具全链路恢复
- [x] 模型下拉加载中显示「加载模型中…」占位（不再空白）
- [x] 清理临时诊断日志
- [x] 结论：models=0 只是启动瞬间（init 未完成），重试机制 2~3 次后 369 模型全部加载

## M15 · 滚动自由（Agent 输出时可上滚查看）✅ 2026-08-18
- [x] 根因：自动贴底逻辑无条件 scrollTop=scrollHeight，流式输出时持续强制贴底 → 用户无法上滚
- [x] 修复：智能贴底（用户上滚 >120px 暂停跟随，滚回底部恢复）+「↓ 回到底部」浮动按钮
- [x] scroll-behavior: smooth 平滑滚动
