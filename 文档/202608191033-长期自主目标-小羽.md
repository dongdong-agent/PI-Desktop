# PI Agent · 长期自主目标（2026-08-19 起）

> 模式（东哥 2026-08-19 定稿）：小羽**自主推进**。能自动验证的（协议/单测/构建/sidecar 冒烟）→ 小羽自主测试、通过后自动进入下一步；无法自动验证的（窗口视觉、交互手感、真实模型体验、安装包实机）→ 完成前置后**汇总清单交东哥验收**，不擅自跳过。
> 优先级来自 202608181539 调研 + 20260818 长期目标 + 用户真实痛点（413 过大 / 长会话卡 / 输出乱）。

---

## 分级说明

- **🔬 可自主测试**：tsc 编译 / vitest 单测 / sidecar 冒烟脚本 / 构建产物——小羽自主完成，不打扰东哥。
- **👁 需东哥验收**：窗口视觉、交互手感、真实模型回复质量、安装包装机运行。
- **P0/P1/P2/P3**：优先级。

---

## M16 · 会话树打包 + 长会话体验（P0）

### M16.1 会话树 /tree（已完成实现，待打包） 🔬
- [x] sidecar `get_tree`（SessionManager.getTree → 精简树）✅
- [x] 前端 TreePanel.tsx：树形渲染 + leaf 高亮 + 分支 fork ✅
- [x] 构建 + 测试 18/18 ✅
- [x] **重新打包**：把会话树打进安装版 ✅（本次已完成）

### M16.2 长会话加载优化（1254 条卡顿） 🔬→👁
- [x] 诊断：当前 `historyToMessages` 全量渲染数千条 DOM → 卡
- [x] 方案：消息区**分组折叠**（默认渲染最近 200 条，更早折叠「⬆ 显示更早」，点击逐步展开 200/次）— 已实现 ✅
- [x] 方案：实测 1254 条不再一次性渲染上万 DOM（窗口化计算单测 4/4）✅
- [ ] 验证：500+ 条会话滚动流畅性（🔬 窗口化单测已过；👁 东哥开 1254 条会话确认）（🔬 用 sidecar 构造大会话测渲染；👁 东哥开 1254 条会话确认）

### M16.3 版本升 0.2.0 🔬
- [x] 版本升 0.2.0（package.json/tauri.conf/Cargo.toml 三处一致）✅
- [x] 重新打包：setup.exe 36MB(11:02) / msi 65.2MB(10:58)，含会话树+长会话优化 ✅

---

## M17 · 上下文管理增强（P1，直接缓解 413 痛点）

### M17.1 自动压缩策略 🔬
- [x] 现状确认：SDK 自动压缩是 token 阈值驱动（contextTokens > window - reserve），413 会话仅 11% 永不触发 → **内核盲区**
- [x] 增强（M17.1 落地）：get_session_stats 增 payloadBytes（序列化全消息字节=上游请求体近似）；状态栏显示「📦 载荷」+ 4MB 警告 / 8MB 危险 + 一键压缩按钮
- [x] 验证：sidecar 冒烟真实会话 1624 条 = 0.51MB 有效；单测 23/23 通过
- [ ] 👁 东哥复现原 413 会话：观察载荷显示与压缩缓解效果

### M17.2 上下文构成可视化 🔬
- [ ] 状态栏 context% 扩展为：输入/输出/缓存/工具 分段条
- [ ] 超阈值预警色（绿色→黄→红）
- [ ] 验证：单测 + 肉眼观感 👁

---

## M18 · 终端能力补齐（TUI 对齐，P1）

### M18.1 fork / clone 分支补齐 🔬→👁
- [x] 根因确认：PI 的 /clone = runtime.fork(leafId, {position:'at'})（从当前 leaf 立即原地分支）
- [x] sidecar fork 支持 position:'at'（clone 语义，无需树选择）
- [x] TreePanel 加「⧉ 克隆当前」按钮（复制当前分支，原链不受影响）
- [x] sidecar 冒烟：clone 成功且生成新会话文件（CLONED_NEW）；单测 23/23

### M18.2 `@` 文件引用 🔬→👁
- [x] sidecar search_files：递归扫描项目文件（忽略 node_modules/.git 等，深度/大小受限），query 子串过滤，浅层优先排序 ✅
- [x] sidecar read_file：项目内文件读取（path.relative 统一分隔符越权校验，限 2MB）✅
- [x] 前端：输入 `@文字` 弹项目文件补全 → 选中插入 @路径；发送时解析 `@路径` → 读文件 → `<file path=...>内容</file>` 注入 prompt ✅
- [x] 验证：sidecar 冒烟（search 60 文件/query 命中 8；read_file 合法✅越权✅空路径✅）+ 单测 23/23
- [ ] 👁 东哥试：输入 @ -> 选文件 -> 发送，看 PI 是否读到文件内容

### M18.3 `!cmd` / `!!cmd` shell 前缀 🔬→👁
- [x] 内核确认：TUI 的 !cmd = session.executeBash(command, onChunk, {excludeFromContext})（!! 时输出不进 LLM 上下文）
- [x] sidecar prompt 检测 `!`/`!!` 前缀 → executeBash + 转发 tool_execution 事件（时间线可见）
- [x] 验证：sidecar 冒烟（!echo 解析✅ !!cmd exclude=true✅ 事件 6 个✅）+ 单测 23/23 + dev 生效
- [ ] 👁 东哥试：输入 !ls 或 !!ls 看 bash 执行与输出显示

---

## M19 · 差异化护城河（P2，我们独有）

### M19.1 技能参数化表单（深化，M8 轻量版已做） 🔬→👁
- [x] **核实**：PI 技能 frontmatter 仅 name+description，无参数 schema → 结构化表单不适用（已如实向東哥说明，转方案 A）
- [x] 深化：SkillDialog.tsx UI 模态（替代 window.prompt）——完整描述 + 指令输入框 + 4 个常用意图快捷模板（分析/总结/执行/审查）
- [x] 确认 → /skill:name 指令 发送
- [x] 构建 + 全量 26/26 + dev 生效
- [ ] 👁 東哥验收：点技能弹窗体验（描述/模板/指令）

### M19.2 桌面宠物行为增强 🔬→👁
- [x] tauri.conf withGlobalTauri:true + capabilities/pet.json（pet 窗口可用 event.listen）
- [x] Rust pet_event 命令（emit pet_event，仅 pet 窗口存在时广播）+ lib.rs 注册
- [x] pet.html：监听 pet_event → body 状态类去重动画（busy 转圈 / think 快浮 / done 缓浮）
- [x] PiChatView：事件批次驱动（agent_start/message_start→busy；thinking_delta/message_delta→think；settle/end→done）
- [x] 构建+单测 23/23+dev 生效；pet.html 已入 dist
- [ ] 👁 东哥验收：设侧栏「呼出桌面宠物」，发送消息看宠物忙碌转圈→思考→空闲
- [ ] 拖拽/点击说话等交互待后续（窗口已可拖拽移动 pet-root -webkit-app-region:drag）

### M19.3 素材库 / 提示词库 🔬→👁
- [x] localStorage(aiwb:materials) JSON 持久化收藏
- [x] 侧栏素材库区：收藏输入框 + 列表 + 删除 + 点击插入
- [x] 插入机制：CustomEvent pi:insert-text → PiChatView 输入框追加
- [x] 单测 3/3 + 全量 23/23 + 构建 + dev 生效
- [ ] 👁 东哥验收：侧栏展开素材库→收藏→点击插入到输入框

---

## M20 · 中文母语化打磨（P2，覆盖不了但重要）

### M20.1 界面与命令中文化 🔬→👁
- [x] 复查结论：界面中文覆盖已良好（relTime 刚刚/分钟/小时/天、tool 状态符号、无英文 title/placeholder 残留）
- [x] 优化：空状态提示中文化（bash/编辑/文件→执行命令/编辑代码/读写文件）、tokens→词元、正在工作→正在处理
- [x] 验证：构建 + 全量 26/26 + dev 生效
- [ ] 👁 東哥肉眼复查

---

## M21 · 工程交付完善（P3）

### M21.1 网页版/部署一致性 🔬
- [ ] 网页预览（vite preview）与桌面版行为对齐（CSP、图片等）

### M21.2 安装包自包含回归 🔬→👁
- [ ] 每轮大改动后重打包，保证 node+pi 捆绑完整
- [ ] 东哥换无 node/pi 机器验证 👁（长期）

---

## 当前执行指针（小羽自主推进队列）

1. **M16.2 长会话优化**（当前，用户体验最大值）
2. M16.3 版本升 0.2.0（随打包一起）
3. M17.1 自动压缩
4. M18.1 clone
5. M18.2 @ 引用
6. M18.3 !cmd
7. M19 差异化
8. M20 中文
9. M21 工程

---

## 需东哥验收的集中清单（每次打包后汇总）

- [ ] 安装包实机安装/启动（装完不发散的场景）
- [ ] 长会话（1254 条）滚动流畅度
- [ ] 会话树分支/回溯手感
- [ ] 413 后点「压缩上下文」是否真缓解
- [ ] 技能参数化表单、宠物动画、素材库界面观感

---

## 纪律（小羽自主）

1. 每一步：改代码 → 🔬 自动验证（tsc / vitest / sidecar 冒烟）→ 通过才进下一步
2. 👁 项不擅自判定通过，集中交东哥
3. 重大方向调整 / 需东哥决策 → 先汇报给东哥，不擅自定
4. 完成一项即在本文件勾选 [x] 并落记录到开发日志
